#include <stdio.h>

int main()
{
  int n,i;
  char str[567];
  printf("enter string");
  scanf("%c",&str[i]);
  for(i=0;str[i]!='\0';i++)
  {
  if(str[i]==(n>=0 && n<=9))
  {
      printf("%d",str[i]);
  }
}
}
